version: '3.7'

services:
  redis:
    image: redis:alpine
    container_name: redis
    networks:
      - services-network
    ports:
      - "6380:6379"
networks:
  services-network:
    external: true